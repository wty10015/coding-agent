# v0.1.0 初始 Alpha 发布说明

状态：已发布。

`v0.1.0` 是我已公开的早期 Alpha 记录，范围是可安装 CLI、Provider 配置、只读工作区检查及独立的安全与状态模块。它不代表完整 Agent runtime 已在该 tag 中交付。

我会保留这个 tag 和 GitHub Release，不重写已公开历史。后续完整运行链路、评测证据和文档会以新的实际日期提交，并以 `v0.2.0-alpha.1` 发布。

安全问题请遵循 SECURITY.md 的私密反馈方式；不要在 Issue、示例或日志中提交真实凭据。
