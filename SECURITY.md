# 安全策略

## 当前范围

本仓库目前公开的是本地 Alpha 模块。安全相关内容包括工作区路径边界、文件读写与 shell 的应用层策略、本地工件脱敏，以及凭据不进入版本控制的约定。我不会在测试或 CI 中读取真实凭据，也不会把真实 Provider 调用作为公开评测的默认路径。

这些策略不替代操作系统权限、容器、虚拟机、网络隔离或人工代码审查。请勿将真实凭据、私有文件或敏感路径作为测试输入。

## 报告漏洞

优先在仓库的 GitHub Security 页面使用 **Report a vulnerability**（Private Vulnerability Reporting）提交报告；请确认该入口可用后再填写漏洞详情。

如果该入口不可用，请只创建一个公开的协调 Issue，例如标题为“Security contact request”。公开内容不得包含漏洞细节、复现步骤、payload、凭据、日志、绝对路径、用户信息或其他敏感材料。等待维护者提供私下沟通方式后，再传递完整报告。

项目尚未承诺固定响应时限或支持周期。请在报告中说明受影响的公开版本、影响概述和安全的联系方式；不要在仓库中粘贴任何 secret。当前 `v0.2.0-alpha.1` 仍是准备中的 Alpha 发布，Provider 配置和工具安全边界可能继续调整。
